# A Simple List

## Instructions

Create an unordered list of three favorite foods.

- Use a `<ul>` containing exactly **three** `<li>` items.
- The first item text must be **Apples**.
- Do not include an ordered list (`<ol>`) on the page.
